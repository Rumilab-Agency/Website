import styled, {  } from 'styled-components';
import { Title, Container } from '../../layouts/AppLayout.style';


export const PortfolioContainer = styled(Container)`
  background-color: black;

`;

export const PortfolioTitle = styled(Title)`
  color:white;

`;